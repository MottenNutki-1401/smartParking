import "../../styles/.css";

function Button({ toggleSidebar }) {
  return (
    <div >
  
      <div className="btn">
     
      
      <button className="btn" onClick={toggleSidebar}>
      </button>
    </div>
    </div>
  );
}
export default Button;